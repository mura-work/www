<h2>みんなの質問</h2>

<div class="row">
  <div class="col-xs-offset-3 col-xs-6">
	<div class="content_box">
	<p>各GSメンバーごとの質問を見る</p>
	<%= form_tag search_books_path, method: :get do %>
	  <%= select_tag 'person_name', options_for_select([["花本静来", "花本静来"], ["和佐田遼", "和佐田遼"],  ["石井直斗", "石井直斗"], ["古谷圭介", "古谷圭介"]]), { :selected => @person_name,  :prompt => "選択してください" } %>
	  <%= submit_tag "絞り込み" %>
	<% end %>
	</div>
  </div>
</div>


<% @bookd %>に寄せられた質問






<div class="row">
	<div class="col-xs-offset-1 col-xs-10">
		<div class="main_contents">
			<% @books.each do |book| %>
				<div class="box2">
				<pre><%= link_to book.body, book_path(book.id) %></pre>
				<div class="category">ジャンル: <%= raw(book.tag_list.map { |t| link_to t, tag_path(t) }.join(', ')) %></div>
				<span class="comment-meta">
				  <span class="comment-date"><%= book.created_at.strftime('%Y/%m/%d') %></span>
				</span>
				<div class="name">回答者:<%= book.person_name %></div>
<!-- ------------------------聞きたいボタン---------------------------------- -->

				<%= render partial: 'books/books', locals: {book: book} %>

<!-- -----------------------------聞きたいボタン----------------------------- -->
				</div>
			<% end %>
		</div>
	</div>
	<div class="col-xs-1"></div>

</div>

<%= paginate @books, class: "paginate" %>








